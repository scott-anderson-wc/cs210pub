rubric_data = {
    'assign_num': 'A03',
    'assign_desc': 'Zodiac.js JavaScript and JQuery',
    items: [
        {value: 10,
         desc: 'makeDate() takes a string as its argument and returns a date object'},
        {value: 10,
         desc: ' formatDate() takes a date object and returns a formatted string '},
        {value: 10,
         desc: ' zodiacSign() takes a date object and returns name of the zodiac sign'},
        {value: 10,
         desc: ' randomElt() takes an array and returns a random element from the array'},
        {value: 10,
         desc: ' randomDate() randomly chooses a date string from an array of test values '},
        {value: 10,
         desc: ' updatePage() puts the above functions together and inserts the correct image into the page.'},
        {value: 10,
         desc: ' Zodiac sign and date appear when page loads and when button is clicked'},
        {value: 10,
         desc: 'test_makeDate() invokes makeDate() for every element of a test value array and is printed to the console'},
        {value: 10,
         desc: 'test_formatDate() invokes formatDate() for every date derived from your array of test values and prints the result to the console.'},
        {value: 10,
         desc: 'test_zodiacSign() invokes zodiacSign() for every date and prints the result.'}
        ]
};
